services:
  api:
    build: .
    command: ["uvicorn", "elastisched_api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./elastisched.db
    volumes:
      - ./:/app
